# 内容提交规范

## 新项目

每个项目使用独立目录，并至少包含：项目目标、适用人群、功能说明、运行方法、示例结果和限制。

## 新 Skill

每个 Skill 使用独立目录，并包含完整的 `SKILL.md`。提交前确认触发描述清晰、步骤可执行、示例已脱敏。

## 发布检查

- 不包含 API Key、Cookie、密码或 `.env` 文件。
- 不包含客户姓名、订单、内部数据和未经授权的素材。
- 图片、字体和其他资源具有公开使用权限。
- README 中的命令和链接可以正常使用。

